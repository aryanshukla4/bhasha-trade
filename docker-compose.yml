services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bhasha_trade
      POSTGRES_USER: bhasha_trade
      POSTGRES_PASSWORD: bhasha_trade_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
volumes:
  postgres_data:
  redis_data:
